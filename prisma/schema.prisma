generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Laptop {
  id     Int    @id @default(autoincrement())
  name   String
  rating Float
  specs  String
  price  String
  image  String
}
